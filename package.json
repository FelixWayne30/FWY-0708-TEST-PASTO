{
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "concurrently \"vite --config configs/vite.config.ts\" \"wait-on http://localhost:5173 && electron .\"",
    "build": "tsc && vite build --config configs/vite.config.ts && electron-builder",
    "electron": "electron .",
    "compile": "tsc --project tsconfig.node.json"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "@vitejs/plugin-react": "^4.6.0",
    "concurrently": "^9.2.0",
    "vite": "^6.3.5",
    "wait-on": "^8.0.3"
  }
}
